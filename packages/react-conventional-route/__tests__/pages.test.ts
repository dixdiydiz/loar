import * as path from 'path'
import routes from '../index'

describe('routes', () => {
  it('routes create', async () => {
    // expect(await routes(path.resolve(__dirname, './pages'))).toEqual([])
  })
})
